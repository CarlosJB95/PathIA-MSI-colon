# =====================================================================
#  pathia-msi-colon — dependencias con versiones EXACTAS (==)
#
#  Verificado (2026-09-24) con `pip install --dry-run` que el conjunto
#  resuelve sin conflictos en Python 3.11 y 3.12 (Colab), Linux x86_64.
#  Rango soportado: Python 3.11–3.12 (tiatoolbox exige >=3.11).
#
#  Los techos de varias versiones (torch, numpy, pandas, timm, openslide)
#  los impone tiatoolbox 2.1.3: si actualizas una librería, vuelve a
#  resolver TODO el archivo, no solo esa línea.
# =====================================================================

# --- Núcleo científico ----------------------------------------------
numpy==2.4.3
pandas==3.0.2
matplotlib==3.10.9
seaborn==0.13.2
scikit-learn==1.8.0          # métricas (AUROC, AUPRC), StratifiedGroupKFold
pyyaml==6.0.2                # leer configs/*.yaml
tqdm==4.68.4

# --- Imagen y WSI ---------------------------------------------------
pillow==12.1.1
scikit-image==0.26.0
openslide-python==1.4.3
openslide-bin==4.0.1.1       # binarios de OpenSlide vía pip (no hace falta apt)
tiatoolbox==2.1.3            # lectura de WSI, máscara de tejido y tiling
h5py==3.16.0                 # guardar/leer embeddings por laminilla

# --- Deep learning / modelos fundacionales --------------------------
torch==2.12.1                # ver README: en local puede requerir otra build CUDA
torchvision==0.27.1
timm==1.0.27                 # carga de UNI (hf-hub:MahmoodLab/uni)
huggingface_hub==1.21.0      # login y descarga de modelos gated
transformers==5.14.0         # carga de Phikon-v2 (AutoModel)

# --- Notebooks y calidad --------------------------------------------
jupyter==1.1.1
nbstripout==0.9.1            # limpiar salidas de notebooks antes del commit
pytest==9.1.1                # pruebas en tests/ (p. ej. chequeo de leakage)

# --- MIL / agregación slide-level -----------------------------------
# (CLAM / STAMP / TRIDENT se instalan desde sus repos, no vía pip)
