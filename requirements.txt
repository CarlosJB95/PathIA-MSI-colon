# =====================================================================
#  pathia-msi-colon — dependencias
#  Reproducibilidad: ancla versiones EXACTAS cuando el entorno se
#  estabilice con:   pip freeze > requirements.txt
#  Por ahora se fija un piso mínimo por fase. Python 3.10+.
# =====================================================================

# --- Núcleo científico (Mes 1: Python + EDA + visualización) ---------
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4          # métricas: AUROC, AUPRC, splits
pyyaml>=6.0                # leer configs/*.yaml
jupyter>=1.0
nbstripout>=0.7            # limpiar salidas de notebooks antes del commit

# --- Imagen y WSI (Mes 2 en adelante) — descomenta cuando llegues ----
# opencv-python>=4.9
# openslide-python>=1.3
# openslide-bin>=4.0        # binarios de OpenSlide (si no usas apt/conda)
# Pillow>=10.2
# scikit-image>=0.22
# tqdm>=4.66

# --- Foundation models / DL (Mes 3+ y local, Meses 5-12) -------------
# torch>=2.2                # instala la build con CUDA 12.x en local (RTX 4070)
# torchvision>=0.17
# timm>=0.9                 # backbones; requerido por UNI
# huggingface_hub>=0.24     # acceso a modelos gated (UNI, Phikon)
# transformers>=4.40
# h5py>=3.10                # guardar/leer embeddings

# --- MIL / agregación slide-level (Mes 8+) --------------------------
# (CLAM / STAMP se instalan desde sus repos, no vía pip)
